# Flow

Fractal/permutational language node
fundamental scale formulas r_s / l_p, r_s
typed symbolic representation

# Action

differential symbolic equation over arbitrary F
stored structurally, not as opaque comments

# Maneuvers

grouped Flow + Action meta-schema
Δ(A) as a first-class formula

# United

collection/interchange layer
runtime extraction operator stored explicitly

# Elite

generalized procedural schema
fixed-radius formula r_f

# Follower

.fwp / .fwh / .fwb / .fws-style strongly typed project/header spec
metric tensor/header relation preserved faithfully

# Ether

machine epsilon observation structure
lifecycle states for observation/mitigation